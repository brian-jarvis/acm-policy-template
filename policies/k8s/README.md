#   Non-OpenShift Policies
Directory containing the `Policies` that are available to non-OpenShift clusters.  Depending on environment having different subfolders for each k8s distribution might make sense.
